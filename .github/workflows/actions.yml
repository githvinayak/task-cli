name: task-cli
on: 
  push:
    branches:
      - main
jobs:
  build:
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
          - macos-latest
    runs-on: ${{matrix.os}}
    permissions:
      contents: write
      
    steps: 
      - name: vinayak
        uses: echo "hello"